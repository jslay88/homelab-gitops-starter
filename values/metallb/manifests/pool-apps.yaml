# CHANGEME: dynamic LB pool. Do not overlap nodes, DHCP, or the ingress VIP.
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: apps
  namespace: metallb-system
spec:
  addresses:
    - 10.0.0.50-10.0.0.99
