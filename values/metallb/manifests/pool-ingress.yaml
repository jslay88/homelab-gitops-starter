# CHANGEME: pinned ingress VIP. Keep it out of the CP (.11-.19) and worker (.21-.29) blocks.
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: ingress
  namespace: metallb-system
spec:
  addresses:
    - 10.0.0.30/32
