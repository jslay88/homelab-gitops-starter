# CHANGEME: pinned ingress VIP. Keep it out of CP (.11-.19), API VIP (.20), and workers (.21-.29).
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: ingress
  namespace: metallb-system
spec:
  addresses:
    - 10.0.0.30/32
